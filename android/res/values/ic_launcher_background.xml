<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The plate the medallion sits on, for launchers whose mask is wider than a circle.
         Sampled from the artwork just outside its ring so the two read as one image. -->
    <color name="ic_launcher_background">#CFC7BC</color>
</resources>