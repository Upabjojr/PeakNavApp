package com.peaknav.viewer.render_tiles;

public enum PixmapLayerName {
    // NOTE: order corresponds to drawing priority
    // (last elements will appear on top of the previous ones)
    UNDERLAY_LAYER,
    BASE_ROADS,
    SKI_SLOPES,
    NAVIGATION_LAYER,
}
