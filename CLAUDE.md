# CLAUDE.md

This project's guidance for AI coding agents lives in **[AGENTS.md](./AGENTS.md)**.

Please read it before making changes — it covers the module layout, the
cross-platform architecture (shared `core` + per-platform `NativeScreenCaller` /
`LoadFactory`), build and run commands, coding conventions, and the
internationalization rules.
