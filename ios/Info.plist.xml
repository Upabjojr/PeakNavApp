<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleDisplayName</key>
    <string>${app.name}</string>
    <key>CFBundleExecutable</key>
    <string>${app.executable}</string>
    <key>CFBundleIdentifier</key>
    <string>${app.id}</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>${app.name}</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>${app.version}</string>
    <key>CFBundleSignature</key>
    <string>????</string>
    <key>CFBundleVersion</key>
    <string>${app.build}</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UIViewControllerBasedStatusBarAppearance</key>
    <false/>
    <key>UIStatusBarHidden</key>
    <true/>
    <!-- 12.0, not the 8.0 this carried from the libGDX project template. The gdx natives
         ship as xcframeworks with only ios-arm64 and simulator slices - there is no 32-bit
         armv7 build to target an iOS 8 device with - and the App Store has not accepted a
         deployment target this old for years. 12.0 is the floor for arm64-only, which is
         what is actually being built. -->
    <key>MinimumOSVersion</key>
    <string>12.0</string>
    <key>UIDeviceFamily</key>
    <array>
        <integer>1</integer>
        <integer>2</integer>
    </array>
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>opengles-2</string>
    </array>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>
    <key>CFBundleIconName</key>
    <string>AppIcon</string>
    <!-- NativeScreenCallerIOS.shareSnapshot puts the rendered frame into the system share
         sheet, whose "Save Image" writes to the photo library. iOS terminates an app that
         reaches the library with no usage string in its Info.plist - so the absence of this
         key is not a missing prompt, it is a crash the moment a user shares a snapshot. -->
    <key>NSPhotoLibraryAddUsageDescription</key>
    <string>PeakNav needs this to save a snapshot of the view to your photo library.</string>
    <!-- Without this key CLLocationManager.requestWhenInUseAuthorization() does not fail -
         it silently never shows the prompt, and the "here" button does nothing forever. -->
    <key>NSLocationWhenInUseUsageDescription</key>
    <string>PeakNav uses your location to center the mountain view on where you are.</string>
    <!-- Same class of problem as the photo-library key above, but harsher: reaching
         CoreMotion without a usage string is app termination, not a missing prompt. -->
    <key>NSMotionUsageDescription</key>
    <string>PeakNav uses the motion sensors to aim the view where you point your phone.</string>
    <!-- Read access, distinct from the Add key above: the gallery picker itself needs no
         permission, but the photo's location does - iOS strips GPS EXIF from picked files,
         so "go to where this photo was taken" only works through the library record. -->
    <key>NSPhotoLibraryUsageDescription</key>
    <string>PeakNav reads a chosen photo's location to show the view from where it was taken.</string>
    <!-- Termination, not a prompt, if the camera is reached without it - as with motion. -->
    <key>NSCameraUsageDescription</key>
    <string>PeakNav uses the camera to place a photo behind the mountain view.</string>
</dict>
</plist>
